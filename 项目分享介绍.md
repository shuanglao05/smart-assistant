# 智能个人助理系统 · 分享文案（精简版）

> 对外分享/答辩用，只突出 4 个核心亮点，其余一笔带过。

---

## 项目名

**灵犀 · 智能个人助理**（Lingxi IPAS）——多用户 Web 版 AI 个人助理。

> 一句话：本地/云端大模型随便切，内置技能库定制 AI 人设，日常工具随叫随到，还能直接拿文件问 AI。

---

## 四大亮点

### ① 大模型自由切换
- 聊天顶栏一键切换：**本地 Ollama（qwen3:8b，离线可用）** ⇄ **云端模型（OpenAI 兼容：阿里云百炼/DeepSeek/智谱/OpenAI）**，且**按会话独立记忆**，不同会话可各用各的模型。
- 云端没配好？界面弹窗填 Key/Base URL/模型名即可，**保存前真实测连**，配置错了立刻告诉你原因、不会写坏。
- 打字机流式输出，随时「停止」；答得不好可「⟳ 重新生成」。

### ② Skills 技能库 —— 给你的 AI 装"人设/专长"
- 技能 = 一段指令（名称 + 描述 + Prompt），**勾选即生效**：指令拼进当前会话系统提示词，立刻改变回答风格。
- 例：启用"小红书写手"后，AI 回复自动带 emoji、短句和 #话题。
- 支持**新建/编辑/删除**，也支持**导入 .md 文件或整个技能文件夹**（自动解析 `SKILL.md`），装一个技能库就是一次点击。

### ③ 集成日常生活工具
- **天气**：中文城市名即可，"北京现在天气"实时实况；"北京未来三天"给逐日白天/夜间预报，卡片以 `日期|白天|夜间|风向` 表格展示。
- **计算**：直接问"12 乘以 15"，AI 调用计算器返回 180。
- **待办**：说"帮我记一下明天交报告"，右侧待办面板自动出现，可勾选/删除/看进度。
- **站内提醒**：说"提醒我明天上午 9 点开组会"，AI 把提醒推进右上角通知中心（铃铛红点）。

### ④ 附件问答 —— 拿文件直接问 AI
- 输入框 📎 上传 txt / md / csv / json / pdf（≤8MB，可多选），发送后 AI **基于文件内容**回答；PDF 自动抽取文字。
- 例：上传写有"校训是『知而行远』"的 txt，问"校训是什么" → 正确回答"知而行远"。

---

## 还有这些（一笔带过）

多会话管理（改名/删除/**导出 Markdown**）、**跨会话历史搜索**一键直达、天气/技能/待办三区**可折叠**、左右栏宽度可拖、通知中心已读/清空、JWT 登录与**多用户数据隔离**。

## 技术栈

FastAPI · SQLite · LangChain/LangGraph · Ollama + OpenAI 兼容 API · React 18 + TypeScript + Vite · JWT/bcrypt。

## 快速体验

```
cd backend && venv\Scripts\python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
cd frontend && npm run dev   # http://localhost:5173
```
