# 智能个人助理系统 · 环境准备与软件安装指南

> 适用文档：`开发文档_详细版.md` 第 4 章（目录结构）、第 11 章（环境安装与运行）  
> 项目根目录：`D:\专业实习-IPAS\smart-assistant`  
> 本指南只做**环境准备**，不涉及任何业务代码。



---

## 0. 一句话结论

**你电脑上 90% 的软件已经装好了**，真正要做的只有三件事：

1. 给 VSCode 装 4~6 个插件（一条命令搞定）
2. 在 `backend/` 里建一个 Python 虚拟环境（venv）
3. ~~申请大模型 API Key~~ → **已改用本地 Ollama，不用申请 Key**（云端位置已预留）

数据库**不用装**（SQLite 是 Python 自带的），Redis、Docker、MySQL 全都**不需要**。

---

## 1. 你电脑当前环境（已实际扫描）

| 软件              | 检测到的版本                                                                | 状态     | 说明                  |
| --------------- | --------------------------------------------------------------------- | ------ | ------------------- |
| Python          | **3.13.5**  
`C:\Users\lao06\AppData\Local\Programs\Python\Python313` | ✅ 可用   | 文档要求 3.10+，满足       |
| Node.js         | **v24.12.0**（nvm4w，位于 `C:\nvm4w\nodejs`）                              | ✅ 可用   | 文档要求 18+，满足         |
| npm             | 10.9.7                                                                | ✅ 可用   | 随 Node 一起装的         |
| VSCode          | 1.136.1（`code` 命令可用）                                                  | ✅ 可用   | 只需补插件               |
| Anaconda Python | 3.8.8（`D:\Ana`）                                                       | ⚠️ 陷阱  | **版本太旧且会抢 pip，别用它** |
| Ollama          | 已安装                                                                   | 🟢 白捡的 | 可作为"不用花钱的备选 AI 后端"  |
| Docker Desktop  | 已安装                                                                   | ⚪ 用不上  | 本项目不需要              |
| Git             | 未在系统标准路径找到                                                            | 🟡 可选  | 不影响开发，建议后面补装        |

> ⚠️ **最大的坑**：你的 `pip` 现在指向的是 Anaconda 的 Python 3.8（`D:\Ana\lib\site-packages\pip`）。  
> 3.8 版本跑不了本项目（文档要求 3.10+，且很多库已不支持 3.8）。  
> **解决办法：建虚拟环境后，一律用 `python -m pip install ...`，不要裸敲 `pip install ...`。**

---

## 2. 需要准备的软件清单

### 2.1 必装（缺一不可）

| # | 软件           | 用途         | 你现在的状态                   |
| - | ------------ | ---------- | ------------------------ |
| 1 | Python 3.10+ | 后端运行时      | ✅ 已有 3.13.5，**但需新建虚拟环境** |
| 2 | Node.js 18+  | 前端构建/开发服务器 | ✅ 已有 v24.12.0            |
| 3 | VSCode + 插件  | 写代码的编辑器    | ✅ 已有，**需装插件**            |
| 4 | 大模型 API Key  | AI 助理的"大脑" | ❌ **需要申请**（见步骤 5）        |

### 2.2 可选（装了更顺手，不装也能跑）

| 软件                 | 用途               | 建议                   |
| ------------------ | ---------------- | -------------------- |
| OpenWeatherMap Key | 天气查询工具           | 可选，免费注册，不影响其他功能      |
| Git for Windows    | 版本管理、交作业         | 建议装，课程设计最好有提交记录      |
| SQLite 查看插件        | 直接看 `app.db` 里的表 | 用 VSCode 插件即可，不用另装软件 |
| Thunder Client     | 在 VSCode 里测接口    | 比 Postman 轻量，推荐      |

### 2.3 **不需要**装的（别浪费时间）

- ❌ MySQL / PostgreSQL —— 本项目用 SQLite，Python 自带，零配置
- ❌ Redis —— 文档里用的是内存版 `MemorySaver`
- ❌ Docker —— 本地开发用不上
- ❌ Postman —— VSCode 插件足够
- ❌ Anaconda / Miniconda —— 你已有官方 Python，conda 只会添乱

---

## 3. 详细安装步骤

> 下面所有命令都在 **VSCode 的终端** 或 **PowerShell** 里执行。  
> 打开方式：VSCode → 顶部菜单 `终端` → `新建终端`。

### 步骤 1：安装 VSCode 插件（约 2 分钟）

**方式 A（推荐，一条命令）** —— 复制整行到 PowerShell 里执行：

```powershell
code --install-extension ms-python.python; code --install-extension ms-python.vscode-pylance; code --install-extension esbenp.prettier-vscode; code --install-extension dbaeumer.vscode-eslint; code --install-extension qwtel.sqlite-viewer; code --install-extension rangav.vscode-thunder-client
```

**方式 B（手动）** —— VSCode 左侧方块图标（扩展）→ 搜索名字 → 安装：

| 插件名            | 扩展 ID                          | 作用                     | 必要性             |
| -------------- | ------------------------------ | ---------------------- | --------------- |
| Python         | `ms-python.python`             | Python 语法、调试、虚拟环境识别    | **必装**          |
| Pylance        | `ms-python.vscode-pylance`     | 智能补全、类型提示              | **必装**          |
| Prettier       | `esbenp.prettier-vscode`       | 格式化 TS/TSX 代码          | 推荐              |
| ESLint         | `dbaeumer.vscode-eslint`       | 前端代码检查                 | 推荐              |
| SQLite Viewer  | `qwtel.sqlite-viewer`          | 点开 `app.db` 就能看表和数据    | 推荐              |
| Thunder Client | `rangav.vscode-thunder-client` | 测 `POST /api/chat` 等接口 | 推荐（可替代 Postman） |

> 装完插件后**重启一次 VSCode**，让它识别 Python 解释器。

---

### 步骤 2：确认 Python 版本正确（避开 Anaconda 陷阱）

在终端里执行：

```powershell
python --version
```

**判断标准：**

| 你看到的输出          | 说明                  | 怎么办     |
| --------------- | ------------------- | ------- |
| `Python 3.13.x` | 完美                  | 直接进步骤 3 |
| `Python 3.8.8`  | ❌ Anaconda 抢占了 PATH | 见下方解决方案 |

**如果显示 3.8.8 —— 你的情况（已确认）**：用户 PATH 里只加了 `...\Python\Launcher\`，漏了主程序目录，所以 `python` 落到了 Anaconda 3.8.8。

**解决（二选一，可都做）：**

```powershell
# 方案 B（最快，推荐先做）：用 py 启动器指定版本建 venv，绕开 PATH 问题
#   venv 激活后 python 自动指向 3.13，之后不再受 PATH 影响
py -3.13 -m venv venv

# 方案 A（一劳永逸）：补齐用户 PATH（Win 搜索「编辑账户的环境变量」→ 用户变量 Path → 编辑 → 新建以下两条）：
#   C:\Users\lao06\AppData\Local\Programs\Python\Python313\
#   C:\Users\lao06\AppData\Local\Programs\Python\Python313\Scripts\
#   确定后必须重开所有终端/VSCode 才生效
#   注：用户 PATH 优先级高于系统 PATH，加进去后 python 就会先命中 3.13
```



---

### 步骤 3：创建后端虚拟环境（venv）

> 虚拟环境 = 给这个项目单独隔离出一个"干净 Python"，装的包不会污染全局，也不会被 Anaconda 干扰。

```powershell
# 进入后端目录
cd D:\专业实习-IPAS\smart-assistant\backend

# 创建虚拟环境（看到 3.8.8 就把 python 换成 py -3.13）
python -m venv venv
```

如果 PowerShell 报错 **"无法加载文件 ...Activate.ps1，因为在此系统上禁止运行脚本"**，先执行这一条再重试：

```powershell
Set-ExecutionPolicy -Scope CurrentUser RemoteSigned -Force
```

激活虚拟环境：

```powershell
.\venv\Scripts\Activate.ps1
```

**激活成功的标志：命令行前面出现 `(venv)` 前缀。**

激活后再验证一次（必须都是 3.13.x，且路径指向项目内的 venv）：

```powershell
python --version
python -m pip --version
```

```powershell
# 顺手把 pip 升到最新（国内网络建议加清华源，快很多）
python -m pip install --upgrade pip -i https://pypi.tuna.tsinghua.edu.cn/simple
```

> ✅ 依赖包（fastapi / langchain 那些）**先不用装** —— 等我写好 `requirements.txt` 再一次性装，避免版本冲突。  
> 你今晚只要把 venv 建起来、能激活、显示 `(venv)` 就算过关。

---

### 步骤 4：确认 Node 环境（10 秒）

```powershell
node -v
npm -v
```

期望看到 `v24.x` 和 `10.x`。**如果提示"不是内部或外部命令"**，说明 Node 没进 PATH：

```powershell
# 临时加到当前终端（重开终端失效）
$env:Path += ";C:\nvm4w\nodejs"
```

> 前端的 `npm create vite` 和 `npm install` 等脚手架生成后再执行，现在不用动。

---

### 步骤 5：大模型方案（✅ 2026-09-07 已决策：本地 Ollama）

> **当前决策：主用本地 Ollama `qwen3:8b`，云端（OpenAI 兼容平台）的配置位置预留但暂不启用。**
> 因此**你现在不需要申请任何 API Key**，Key 申请相关的步骤保留在下方备用。

#### 本机实测结果（2026-09-07）

| 项目 | 结果 |
|------|------|
| Ollama 服务 | 0.33.2，运行中 ✅ |
| 已装模型 | `qwen3:8b`（5.2GB / 上下文 40960 / 支持 `tools`+`thinking`）<br>`qwen3:14b`（9.3GB，更强备胎）<br>`bge-m3:latest`（1.2GB，embedding 1024 维，留给后期 RAG） |
| 关闭思考 | 原生端点 `/api/chat` + `think:false` ✅ 生效 |
| 工具调用 | 实测 `12*15` → 正确调用 `calculator` ✅，耗时约 5 秒 |
| 生成速度 | 约 20 token/s（8B Q4） |

#### ⚠️ 实测踩到的坑（决定代码写法）

用 OpenAI 兼容端点 `http://localhost:11434/v1` 传 `think:false` **无效**——返回的 `content` 是空的，思考内容全进了 `reasoning` 字段，还会把 `max_tokens` 吃光。

```json
{"message":{"role":"assistant","content":"","reasoning":"好的，用户只让我回复两个字……"},"finish_reason":"length"}
```

**结论：LangChain 侧必须用 `langchain-ollama` 的 `ChatOllama`（走 Ollama 原生 `/api/chat` 端点），不能用 `ChatOpenAI(base_url=".../v1")`。**
`requirements.txt` 里因此额外增加 `langchain-ollama`。

#### 无独显（纯 CPU）怎么跑

这台机器没有 ≥6GB 显存的独显，Ollama 走 CPU 推理。实测数据：

- 预填（读 prompt）约 60 tok/s，生成约 10 tok/s
- 一次工具调用端到端约 5 秒（`12*15` → calculator → 回复）

**结论：`qwen3:8b` 能用，不用急着换小模型。** 已做的优化（写进 `.env` 默认值）：

- `OLLAMA_THINK=false` —— 省掉思考 token，快 3~5 倍
- `OLLAMA_NUM_CTX=8192` —— 少占内存（默认 40960 太浪费）
- 同时开「前端 + 后端 + 浏览器 + 模型」，建议内存 ≥16GB

真觉得卡，按顺序降级：

1. 换小模型：`ollama pull qwen3:4b`，然后 `ollama show qwen3:4b` 确认 capabilities 里含 `tools`，再把 `.env` 的 `OLLAMA_MODEL` 改成 `qwen3:4b`
2. 切云端兜底：`.env` 里 `LLM_PROVIDER=cloud` + 填 `CLOUD_API_KEY`

#### 配置设计（写代码时按此实现）

`backend/.env` 里的关键项：

```env
# 大模型：ollama（本地） / cloud（云端 OpenAI 兼容平台）
LLM_PROVIDER=ollama

# ---- 本地 Ollama ----
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen3:8b          # 工具调不准可改 qwen3:14b
OLLAMA_THINK=false             # 关掉思考，响应快 3~5 倍
OLLAMA_NUM_CTX=8192            # 降低显存/内存占用

# ---- 云端（预留，填了 Key 并把 LLM_PROVIDER 改成 cloud 即生效）----
CLOUD_API_KEY=
CLOUD_BASE_URL=                # 留空=OpenAI 官方；智谱填 https://open.bigmodel.cn/api/paas/v4/
CLOUD_MODEL=gpt-4o-mini

# ---- Embedding（后期 RAG 用，现在只是占位）----
EMBED_MODEL=bge-m3
```

切换云端只需要改 `LLM_PROVIDER=cloud` 一行，业务代码不用动。

---

#### 备用：云端 API Key（现在不用申请，需要时再看）

没有 Key，云端 AI 助理无法生成任何回复。三选一，按推荐度排序：

#### 方案 A：国内 OpenAI 兼容平台（**推荐，最省事**）

这些平台提供和 OpenAI 一模一样的接口，我在代码里预留 `OPENAI_BASE_URL` 配置即可直接切换，不用改逻辑。

| 平台               | 注册地址                                 | 填写的 base_url                                        | 备注                    |
| ---------------- | ------------------------------------ | --------------------------------------------------- | --------------------- |
| 智谱 GLM           | <https://open.bigmodel.cn>           | `https://open.bigmodel.cn/api/paas/v4/`             | 有免费额度（GLM-4-Flash 免费） |
| DeepSeek         | <https://platform.deepseek.com>      | `https://api.deepseek.com`                          | 便宜，工具调用能力好            |
| 硅基流动 SiliconFlow | <https://cloud.siliconflow.cn>       | `https://api.siliconflow.cn/v1`                     | 注册送额度，有免费模型           |
| 阿里百炼（通义）         | <https://bailian.console.aliyun.com> | `https://dashscope.aliyuncs.com/compatible-mode/v1` | 兼容模式                  |

操作：注册 → 实名/登录 → 控制台「API Keys」→ 新建密钥 → 复制保存（**只显示一次**）。

#### 方案 B：OpenAI 官方

- 地址：<https://platform.openai.com/api-keys>
- 需要：境外手机号注册 + 外币信用卡 + 网络环境
- base_url 留空（用默认）

#### 方案 C：本地 Ollama（**你已装了 Ollama，零成本备选**）

```powershell
# 拉一个支持工具调用的模型（约 4~5GB，需等待下载）
ollama pull qwen2.5:7b

# 下载完验证
ollama list
```

- base_url：`http://localhost:11434/v1`
- api_key：随便填，比如 `ollama`
- **优点**：不花钱、不断网、数据不出本机
- **缺点**：7B 小模型调用工具（计算器/天气/待办）时**准确率明显不如云端大模型**，可能答非所问。课程设计演示够用，但验收"工具调用"功能时建议用方案 A。

> 💡 **我的建议**：课程设计主用方案 A（智谱免费额度或 DeepSeek，几块钱够用），Ollama 留作 Plan B。  
> 拿到 Key 后先别填，等我把 `.env.example` 和 `config.py` 写好，你直接把 Key 粘进 `backend/.env` 就行。

---

### 步骤 6：申请天气 API Key（可选，5 分钟）

1. 打开 <https://home.openweathermap.org/users/sign_up> 注册（免费）
2. 登录后进 <https://home.openweathermap.org/api_keys> 复制 Key
3. ⚠️ **新 Key 需要 10 分钟 ~ 2 小时才生效**，很多人以为 Key 错了其实只是没激活
4. 注意：OpenWeatherMap 查城市要用**英文名**，比如 `Beijing` 而不是"北京"

> 不想折腾可以跳过，天气工具会自动返回"未配置 Key"，不影响其他 4 个功能。  
> 想用中文城市名，也可以改用**和风天气**（<https://dev.qweather.com，免费版够用），到时我改一下> `tools.py` 就行。

---

### 步骤 7：项目目录结构（已创建完成）

```
D:\专业实习-IPAS\smart-assistant\
├── backend\
│   ├── app\              # 后端代码（config/database/models/routes...）
│   │   └── .gitkeep
│   ├── requirements.txt  # 待我生成
│   └── .env              # 待你填 Key
├── frontend\
│   └── src\
│       ├── api\          # axios 封装
│       └── components\   # React 组件
├── docs\                 # 可把《开发文档_详细版.md》移进来
└── 安装指南_环境准备.md   # 本文档
```

> `.gitkeep` 只是占位文件，用来保证空文件夹能被 Git 记录，后面写代码时可以删掉。

---

## 4. 今晚的 5 分钟待办清单

按顺序做完打勾，做完回来告诉我一声，我就开始写代码：

- [ ] **1.** 执行步骤 1 的插件安装命令（6 个插件），重启 VSCode
- [ ] **2.** 执行 `python --version`，确认显示 `3.13.x`（不是 3.8.8）
- [ ] **3.** 在 `backend\` 下建好 venv，激活后命令行出现 `(venv)`
- [ ] **4.** 执行 `node -v`，确认显示 `v24.x`
- [ ] **5.** ~~申请大模型 API Key~~ → 已确定用本地 Ollama（`qwen3:8b`），无需 Key；确认 `ollama` 服务处于运行状态即可（`ollama list` 能看到模型）
- [ ] **6.** （可选）申请 OpenWeatherMap Key

**告诉我这些信息，我就能开工：**

```
Python 版本：（例：3.13.5）
Node 版本：（例：v24.12.0）
venv 是否建好并激活：（是 / 否）
大模型平台：（智谱 / DeepSeek / 硅基流动 / OpenAI / Ollama）
模型名：（例：glm-4-flash / deepseek-chat / qwen2.5:7b）
天气 Key：（有 / 没有，跳过）
```

---

## 5. 已知坑预警（必读，能省你两小时）

| # | 坑                                                                                                          | 后果                                   | 预防                                                            |
| - | ---------------------------------------------------------------------------------------------------------- | ------------------------------------ | ------------------------------------------------------------- |
| 1 | **`pip` 指向 Anaconda 3.8**                                                                                  | 包装到 3.8 里，运行时报 `ModuleNotFoundError` | 激活 venv 后一律用 `python -m pip install`                          |
| 2 | **Python 3.13 + `passlib[bcrypt]` 冲突**  
报错 `AttributeError: module 'bcrypt' has no attribute '__about__'` | 注册/登录直接 500                          | 我会在代码里改用 `bcrypt` 库直接哈希，**绕开 passlib**（功能完全一样，文档里的写法不动脑照抄会踩坑） |
| 3 | PowerShell 禁止运行脚本                                                                                          | 无法激活 venv                            | `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned -Force`  |
| 4 | OpenWeatherMap 新 Key 未生效                                                                                   | 天气一直返回"未找到城市"                        | 等 1~2 小时；城市名用英文                                               |
| 5 | 端口被占用（8000 / 5173）                                                                                         | 启动失败                                 | 换端口或关掉占用程序（`netstat -ano \| findstr :8000`）                   |
| 6 | `duckduckgo-search` 库不稳定                                                                                   | 搜索工具导入失败                             | 它是 P2 优先级功能，装不上可以先注释掉，不影响主流程                                  |
| 7 | 项目路径含中文/空格                                                                                                 | 少数工具链编码出错                            | 已规避：项目根用 `smart-assistant` 纯英文，上层中文路径不影响                      |

---

## 6. 下一步

环境 ready 后，我会按文档第 12 章的节奏推进：

| 阶段   | 内容        | 产出                                                                |
| ---- | --------- | ----------------------------------------------------------------- |
| 阶段 0 | 补齐配置文件    | `requirements.txt`、`.env.example`、`package.json`、`vite.config.ts` |
| 阶段 1 | 数据库与模型    | `database.py`、`models.py` → 生成 `app.db` 4 张表                      |
| 阶段 2 | 认证        | `config.py`、`schemas.py`、`deps.py`、`auth.py`                      |
| 阶段 3 | 会话管理      | `sessions.py`                                                     |
| 阶段 4 | Agent 与工具 | `tools.py`、`agent_manager.py`                                     |
| 阶段 5 | 聊天接口      | `chat.py`、`main.py`                                               |
| 阶段 6 | 前端        | `Login` / `ChatContainer` / `SessionList` / `ChatWindow`          |
| 阶段 7 | 联调测试      | 按第 13 章用例逐条验收                                                     |
