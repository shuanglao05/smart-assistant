# 智能个人助理系统 · 项目结构清单与框架解析

> 面向初学者。本文做三件事：
> 1. 逐文件说明每个文件的作用、属于哪个模块；
> 2. 重点剖析 `backend/` 与 `frontend/src/` 的代码分工；
> 3. 用大白话讲清 **LangChain / LangGraph 是怎么搭起来的**，核心代码已加逐行中文注释（见对应 `.py` 文件）。
>
> 技术栈：前端 React + TypeScript + Vite；后端 FastAPI + SQLAlchemy + SQLite；AI 层 LangChain + LangGraph。

---

## 一、整体架构（一句话版）

```
浏览器(React)  ──HTTP/JSON(SSE)──▶  FastAPI  ──▶  LangGraph Agent(大模型+工具)
   │                                        │
   └────────  localStorage(主题/语言/token)  └──▶  SQLAlchemy  ──▶  SQLite(app.db)
```

- **前端**负责：界面、登录态、把用户消息发到后端、把 AI 回复"打字机"式渲染出来。
- **后端**负责：校验身份(JWT)、存消息、调用大模型、把工具结果拼回对话。
- **AI 层**（`agent_manager.py` + `tools.py` + `chat.py`）负责：把"大模型"和"计算器/天气/待办等工具"组装成一个会自动思考、会自动调工具的智能体。

---

## 二、顶层文件清单

| 文件 / 目录 | 作用 | 所属模块 |
|---|---|---|
| `README.md` | 项目总体说明、运行方式 | 文档 |
| `安装指南_环境准备.md` | 环境准备（Python/Node/依赖安装）详细步骤 | 文档 |
| `项目分享介绍.md` | 课程设计答辩/分享用的简介 | 文档 |
| `start.bat` | Windows 一键启动脚本（先后台 uvicorn、再起 vite） | 启动 |
| `.gitignore` | Git 忽略规则（已补 `*.log`；忽略 venv/`__pycache__`/`.env`/`uploads`/`*.db`/`node_modules`/`dist`） | 工程配置 |
| `backend/` | 后端全部代码（见第三节） | 后端 |
| `frontend/` | 前端全部代码（见第四节） | 前端 |

> 已删除的冗余文件：`backend/app/.gitkeep`、`frontend/src/api/.gitkeep`、`frontend/src/components/.gitkeep`、`docs/.gitkeep`（空占位，所在目录本就不空）、`backend/uvicorn.log`（运行日志，会自动再生）、以及空的 `docs/` 目录。
> **保留**的关键数据文件：`backend/app.db`（数据库）、`backend/.env`（密钥配置）、`backend/uploads/`（用户上传）、`backend/venv/`（虚拟环境）、`__pycache__/`、`node_modules/`（均为生成物/被忽略，删了会再生）。

---

## 三、backend/ 后端逐文件清单

### 3.1 根目录

| 文件 | 作用 | 模块 |
|---|---|---|
| `app.db` | SQLite 数据库文件（运行时自动建表；**不要删**） | 数据 |
| `.env` | 环境变量：JWT 密钥、模型供应方、Ollama/云端地址、天气 Key（**不要删，已在 .gitignore**） | 配置 |
| `.env.example` | `.env` 的填空模板，供他人照抄 | 配置 |
| `requirements.txt` | Python 依赖清单（fastapi / langchain / langgraph / sqlalchemy 等 30 行） | 依赖 |
| `uvicorn.log` | 运行日志（**已删除**，再生） | 运行产物 |

### 3.2 `app/` 包（核心代码）

| 文件 | 作用 | 模块 | 是否 AI 核心 |
|---|---|---|---|
| `__init__.py` | 空文件，标记为 Python 包（让 `from app import config` 能成立） | 工程 | — |
| `main.py` | FastAPI 入口：创建 app、注册 CORS、挂载所有路由、启动建表、提供 `/api/health` 与 `/api/llm-options` | 入口 | — |
| `config.py` | **集中配置**：只在这里读 `.env`；含模型供应方、Ollama/云端参数、`SYSTEM_PROMPT`（Agent 的系统提示词）、`llm_options()` 可选模型列表 | 配置 | ★ |
| `database.py` | 数据库连接 + `SessionLocal` + `init_db()` 自动建表与"增量 ALTER 补列"迁移 | 数据 | — |
| `models.py` | **ORM 模型**：`User / Conversation / Message / TodoItem / Skill / FileItem / Notification` | 数据 | ★ |
| `schemas.py` | **Pydantic 请求/响应模型**：`ChatRequest / MessageOut / SessionOut / UserOut …` | 接口契约 | ★ |
| `deps.py` | 依赖注入：解析 JWT 得到"当前登录用户"（`get_current_user`） | 鉴权 | — |
| `auth.py` | 注册 / 登录，签发 JWT（`/api/auth/register`、`/api/auth/login`） | 鉴权 | — |
| `users.py` | 个人资料读取与修改（昵称/头像/语言/主题/改密）`/api/users/me` | 用户 | — |
| `sessions.py` | 会话(Conversation)的增删改查 + 拉取某会话的消息列表（含把 `ref_file_ids` 还原成 `ref_files`） | 会话 | — |
| `chat.py` | **聊天路由**：`POST /api/chat`（非流式）与 `POST /api/chat/stream`（SSE 流式）；处理 regenerate、附件注入、调用 Agent、回写消息 | 聊天 | ★ |
| `agent_manager.py` | **LangGraph Agent 工厂**：`build_llm` 建大模型、`_create_agent` 组装 Agent、`get_agent_for_conversation` 按会话缓存 + 挂记忆；这是整个 AI 层的"心脏" | Agent | ★★ |
| `tools.py` | **LangChain 工具集**：`calculator` / `get_weather` / `get_weather_forecast` / `search_web` 以及按用户隔离的 `make_todo_tools` / `make_notification_tools` | 工具 | ★★ |
| `files.py` | 文件上传、列表、详情（`/api/files`），上传时抽取正文存 `content` 供聊天注入 | 文件 | — |
| `skills.py` | 用户自定义"技能"的 CRUD（技能 = 拼进系统提示词的额外指令） | 技能 | — |
| `todos.py` | 待办事项 CRUD（`/api/todos`） | 待办 | — |
| `notifications.py` | 站内通知（铃铛）接口（`/api/notifications`） | 通知 | — |
| `weather.py` | 天气查询接口（前端天气页直连，独立走高德/OpenWeatherMap） | 天气 | — |
| `search.py` | 跨会话/消息的搜索（`/api/search`） | 搜索 | — |
| `llm_config.py` | 运行时切换/配置大模型（写 `.env` 并内存立即生效） | 配置 | — |
| `api_keys.py` | 设置面板展示当前云端 API 配置（Key 脱敏） | 配置 | — |

> 路由挂载顺序在 `main.py`：`auth → sessions → skills → todos → weather → files → search → notifications → users → api_keys → llm_config → chat`。

### 3.3 数据层三张主表（初学者版）

- **`User`**：一个账号。含 `username`（登录名）、`hashed_password`、`nickname`、`avatar`、`language`、`font_size`、`theme`。
- **`Conversation`**（会话）：一次连续聊天。含 `user_id`（**谁的子会话**）、`title`、`provider`/`model`（用哪个模型）、`active_skill_ids`（启用的技能）。
- **`Message`**（消息）：一条对话记录。含 `conversation_id`、`role`（`user`/`assistant`）、`content`、`ref_file_ids`（本条用户消息引用了哪些附件文档）。
- 其余：`TodoItem`（待办）、`Skill`（技能）、`FileItem`（上传文件）、`Notification`（通知），全都带 `user_id` 实现多用户隔离。

**多用户隔离的根**：几乎所有查询都带 `user_id` 过滤（如 `chat.py` 先查 `Conversation` 再校验 `user_id == current_user.id`），否则会越权看到别人的数据。

---

## 四、frontend/src/ 前端逐文件清单

### 4.1 根级文件

| 文件 | 作用 | 模块 |
|---|---|---|
| `main.tsx` | 入口：挂载 React，渲染前先按 `localStorage.theme` 设主题（防闪白） | 入口 |
| `App.tsx` | 顶层：登录态判断、登录后拉 `/users/me` 用服务端偏好覆盖主题/语言/字号、用 `MainLayout` 作为通配路由 | 路由/状态 |
| `i18n.tsx` | 中英文切换（`I18nProvider` + `SUPPORTED_LANGS`） | 国际化 |
| `types.ts` | 全局 TypeScript 类型（`Message`、`Session`、`Todo`、`Skill`、`FileDetail`…） | 类型 |
| `styles.css` | 全局样式（含 CSS 变量 `--theme`、功能栏拖拽样式、文档查看抽屉、计时器三格输入等） | 样式 |

### 4.2 `api/`（与后端通信）

| 文件 | 作用 |
|---|---|
| `client.ts` | 一个 axios 实例（`baseURL=/api`），请求拦截自动带 JWT，响应拦截 401 踢回登录 |
| `index.ts` | 把所有接口按模块封装成 `authApi / sessionApi / chatApi / todoApi / weatherApi / skillsApi / filesApi / searchApi / notificationApi / usersApi / apiKeysApi` |

### 4.3 `components/`（UI 组件）

| 组件 | 作用 |
|---|---|
| `MainLayout.tsx` | 主布局：左侧会话列表 + 中间聊天/功能页 + 右侧功能栏，按 `pathname` 决定中间显示什么 |
| `Login.tsx` | 登录 / 注册表单 |
| `SessionList.tsx` | 左侧会话列表（新建/切换/删除/改名） |
| `ChatWindow.tsx` | 聊天主窗口：发消息、流式接收、渲染气泡；上方显示引用的文档 chip；可自由滚动查看历史 |
| `MessageBubble.tsx` | 单条消息气泡（用户/助手不同样式，含"重新生成"） |
| `DocViewer.tsx` | 点击文档 chip 后从右侧滑出的文档详情抽屉（Markdown 渲染 + 下载） |
| `FunctionPanel.tsx` | 右侧功能栏（计时器/待办/天气/技能…），**支持拖拽排序**，顺序存 `localStorage` |
| `NotificationBell.tsx` | 右上角铃铛，显示未读通知数 |
| `PageShell.tsx` | 功能页通用外壳（标题 + 内容容器） |
| `SettingsModal.tsx` | 设置弹窗（昵称/头像/语言/主题/字号/模型切换/云端 Key） |
| `SkillCards.tsx` / `SkillsPanel.tsx` | 技能卡片与技能管理面板 |
| `TodoTool.tsx` | 待办组件（内嵌于功能栏或待办页） |
| `Weather.tsx` | 天气组件 |
| `CloudConnectModal.tsx` | 云端模型连接弹窗 |

### 4.4 `pages/`（功能页，由 MainLayout 按路由渲染）

| 文件 | 作用 |
|---|---|
| `TimerPage.tsx` | 计时器（显示 时:分:秒；自定义拆成 时/分/秒 三格输入） |
| `TodosPage.tsx` | 待办页 |
| `SkillsPage.tsx` | 技能页 |
| `WeatherPage.tsx` | 天气页（直连 `weatherApi`） |
| `CalendarPage.tsx` | 日历页（日程/提醒展示） |

### 4.5 前端构建配置（简述）

`package.json`（依赖与脚本）、`vite.config.ts`（Vite 开发服务器，代理 `/api` 到后端）、`tsconfig.json`（TS 配置）、`index.html`（HTML 模板）。这些由脚手架生成，一般无需改动。

---

## 五、★ LangChain / LangGraph 是怎么搭起来的（重点，初学者版）

### 5.1 三个关键概念

1. **LLM（大模型）= 大脑**：接收"消息列表"，吐出"回复"。在 `build_llm()` 里构建，支持本地 Ollama 或云端 OpenAI 兼容。
2. **Tool（工具）= 手脚**：计算器、查天气、记待办……模型自己不会算数/联网，但能"决定调用工具"，工具跑完把结果还给模型。用 `@tool` 装饰普通函数即可（见 `tools.py`）。
3. **Agent（智能体）= 大脑+手脚的自动循环**：LangGraph 的 `create_react_agent` 把"大脑"和"手脚"捆在一起，自动循环：**想 → 调工具 → 看结果 → 再想 → 回答**。

### 5.2 一条消息的完整旅程（看 `chat.py`）

```
用户发消息
  │
  ▼
chat.py 的 /api/chat 或 /api/chat/stream
  ├─ _prepare_conversation()：把用户消息写库（如果是 regenerate 就删掉旧回答重来）
  ├─ _attachment_context()：把引用的附件正文拼到问题前面
  │
  ▼
get_agent_for_conversation(conv.id, user_id)   ← agent_manager.py
  ├─ 先查 agent_cache 有没有现成的（有就直接用，省开销）
  ├─ 没有：build_llm() 建大脑 + 组装 tools + 拼系统提示词 → _create_agent()
  └─ 用 (会话, 模型, 技能) 当缓存键存起来
  │
  ▼
agent.invoke(...)  或  agent.stream(...)
  ├─ thread_id = 会话 id  →  LangGraph 用 MemorySaver 记住这个会话的历史
  ├─ 内部循环：模型看消息 → 要算数就调 calculator → 结果回填 → 继续 → 最终回答
  │
  ▼
把 assistant 回复写库，返回给前端（流式版用 SSE 逐字推）
```

### 5.3 代码对应表（你加过注释的地方）

| 你想懂什么 | 看哪个文件哪段 |
|---|---|
| 大模型怎么建、本地/云端怎么切换 | `agent_manager.py` → `build_llm()` |
| ReAct Agent 怎么组装、记忆(MemorySaver)挂哪 | `agent_manager.py` → `_create_agent()` |
| 为什么每个会话记忆独立、缓存怎么命中 | `agent_manager.py` → `get_agent_for_conversation()`（缓存键含 `thread_id` 来源） |
| `@tool` 把函数变成模型可调用工具 | `tools.py` → `calculator` 上方的注释 |
| 多用户数据隔离（闭包工厂） | `tools.py` → `make_todo_tools()` / `make_notification_tools()` |
| 非流式怎么调、`thread_id` 怎么传 | `chat.py` → `chat()` 里的 `agent.invoke(...)` |
| 打字机流式怎么实现（SSE） | `chat.py` → `chat_stream()` 里的 `agent.stream(..., stream_mode="messages")` |
| 表结构、字段含义 | `models.py` 每个 class 上方注释 |
| 接口入参/出参格式 | `schemas.py` 每个 class 上方注释 |

### 5.4 记忆是怎么"接得上"的（最容易懵的点）

LangGraph 的 `MemorySaver` 会把一个 `thread_id` 下的全部消息状态存在内存里。
我们在 `chat.py` 调用时写死 `config={"configurable": {"thread_id": str(conv.id)}}`——
**用"会话 id"当记忆身份证**。于是：

- 同一个会话多轮提问 → 同一个 `thread_id` → 记忆共享 → 模型记得你前面说了啥。
- 不同会话 → 不同 `thread_id` → 记忆互不干扰。
- `agent_cache` 又把"建好的 Agent 对象"按 `(会话, 模型, 技能)` 缓存，避免每次提问都重建。

### 5.5 工具为什么"安全"且"多用户隔离"

- **计算器不用 `eval`**：`tools.py` 用 `ast` 白名单解析表达式，只认 `+ - * / ** % //` 和正负号，杜绝执行任意代码。
- **待办/通知按用户隔离**：`make_todo_tools(user_id)` 是"工厂"——外层先捕获住当前 `user_id`，返回的两个 `@tool` 内部自动用这个 `user_id` 落库。模型只传 `task`，不知道也不关心"这是谁的"。

### 5.6 换模型要改代码吗？

**不用。** 只改 `backend/.env` 的 `LLM_PROVIDER`（`ollama` / `cloud`）和相关地址/Key，`config.py` 读出来，`build_llm()` 自动选本地或云端实现。业务代码零改动。

---

## 六、本次"结构优化"改动小结

1. **删除冗余**：4 个空 `.gitkeep` 占位、空的 `docs/` 目录、`uvicorn.log` 运行日志。
2. **`.gitignore` 增加 `*.log`**：运行日志不再误提交。
3. **核心代码加初学者注释**：`agent_manager.py`、`tools.py`、`chat.py`、`models.py`、`schemas.py` 已补充逐行/逐段中文注释（不影响任何逻辑，5 个文件 `py_compile` 全部通过）。

> 说明：`__pycache__/`、`node_modules/`、`backend/venv/`、`dist/` 等是编译/依赖生成物，已被 `.gitignore` 忽略且会自行再生，未做删除（删了无意义）。
