# 更新日志

本项目所有值得记录的变更都写在这里。

- **版本号唯一来源**是仓库根目录的 `VERSION` 文件，`GET /api/health` 会返回它。
- 格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，日期为 `YYYY-MM-DD`（东八区）。
- 每个版本内部按 **新增 / 修复 / 变更 / 文档 / 工程** 分组；未涉及的组不出现。
- 需要**技术细节与定位过程**（问题怎么发现的、代码改在哪）时，见
  [`docs/开发文档_详细版.md`](docs/开发文档_详细版.md) 的「更新记录」章节与
  [`docs/答辩与面试问答手册.md`](docs/答辩与面试问答手册.md)。

## 版本总览

| 版本 | 日期 | 主题 |
|---|---|---|
| [v1.5.1](#v151--2026-09-14) | 2026-09-14 | 交互细节打磨、代码注释补全、构建环境兼容修复 |
| [v1.5.0](#v150--2026-09-13) | 2026-09-13 | 多 API 接入、数据目录管理、主题系统 |
| [v1.4](#v14--2026-09-13) | 2026-09-13 | RAG 分片算法重写、PDF 换行归一化、SQLite WAL |
| [v1.3.0](#v130--2026-09-12) | 2026-09-12 | 云端性能修复（42s→0.8s）、日历节假日、知识库增强 |
| [v1.2.0](#v120--2026-09-11) | 2026-09-11 | RAG 知识库、多模型切换、图片识别、思考过程展示 |
| [v1.1](#v11--2026-09-10) | 2026-09-10 | UI 全站改造（DeepSeek 蓝白风 + 线性图标） |
| [v1.0.0](#v100--2026-09-09) | 2026-09-09 | 初始版本：对话 / 技能 / 待办 / 计时器 / 日历 |

---

## v1.5.1 · 2026-09-14

> 交互细节打磨 + 代码注释补全 + 一个构建环境兼容问题的修复。**无需重建数据库**。

### 新增

- **对话导出支持 4 种格式**：Markdown / 纯文本 / JSON / 网页（`.html`，浏览器里可另存为 PDF）。
  顶栏「导出」改为下拉菜单，点击空白处或按 `Esc` 自动关闭。
  其中 JSON 为结构化数据（含角色、时间、来源、引用文件），便于二次处理。
- **设置页新增「上下文窗口」区块**：本地 Ollama 的 `num_ctx` 可在界面上调节
  （预设 2K/4K/8K/16K/32K/64K/128K + 数字输入框，范围 512~131072）。
  保存后清除 Agent 缓存，**下一条消息即刻生效，无需重启后端**。
  配套新增接口 `GET/POST /api/llm-config/context-window`。
  > 云端模型的上下文长度由平台决定、请求里没有对应参数，因此**只对本地开放**，
  > 界面上对这一点做了明确标注，不做"看着能调其实没用"的假控件。

### 修复

- **计时器倒计时到点不响铃**
  两个原因叠加：① 浏览器自动播放策略要求 `AudioContext` 必须在**用户手势**中创建或解锁，
  而到点是定时器回调，此时新建的音频上下文会被拒绝发声；② 到点后紧跟的 `alert()`
  **阻塞主线程**，把铃声一起推迟到弹窗关闭之后。
  改法：全局复用同一个 `AudioContext`，并在用户点「开始」时先调用 `unlockAudio()` 解锁；
  同时去掉阻塞式 `alert`（视觉提示改由按钮状态 + 站内通知承担）。
- **模型名称显示不全且无提示**：模型名过长时虽已省略号截断，但缺少 `title` 属性。
  为模型选择器列表项、当前模型、设置页 API 列表 / Ollama 列表补上 `title`，
  由项目的**全局 Tooltip 组件**统一渲染完整名称（该组件监听页面上所有带 `title` 的元素）。
- **修复 Smart App Control 拦截原生扩展导致后端无法启动**（本版最重要的一处，见下）

### 修复详情：`应用程序控制策略已阻止此文件`

**现象**：后端在 import 阶段直接崩溃，与模型、网络、业务代码均无关。

```
ImportError: DLL load failed while importing _uuid_utils:
             应用程序控制策略已阻止此文件。   (WinError 4551)
```

调用链：
`app.main` → `calendar_api` → `agent_manager` → `langgraph.checkpoint.sqlite`
→ `langchain_core.runnables.config` → `callbacks.manager` → `utils.uuid` → `uuid_utils.compat`

**根因**：`uuid_utils` 的核心是**未签名**的 Rust 原生扩展 `.pyd`。
Windows **智能应用控制（Smart App Control）** 处于强制拦截模式时会拒绝加载这类二进制
（注册表 `HKLM\SYSTEM\CurrentControlSet\Control\CI\Policy` 的
`VerifiedAndReputablePolicyState = 1`）。而 `langchain-core` 与 `langsmith`
都是**硬导入 `uuid_utils` 且没有回退**，于是整个后端起不来。

> **为什么"昨天还能用"**：SAC 新启用时会先经历一段**评估期**（只观察、不拦截），
> 评估结束后自动切换为强制拦截 —— 所以这类故障会在某一天毫无征兆地出现。

**修复**（不动系统设置、完全可回退）：

1. 新增 `backend/patches/uuid_utils_pure/_uuid_utils.py`
   —— 纯 Python 实现：RFC 9562 的 `uuid6/7/8` 自行实现，
   `uuid1/3/4/5` 复用标准库；覆盖原生模块导出的全部符号。
   `uuid7` 采用 RFC 9562 的 Method 1（42 位计数器 + 线程锁），保证**同毫秒内严格递增**。
2. 新增 `backend/fix_sac_uuid_utils.py`
   —— 应用 / 还原 / 诊断三合一脚本：把原生 `.pyd` 改名为 `*.pyd.sac-blocked`
   （文件保留，可随时还原），再放入纯 Python 实现。
   > ⚠️ **必须改名而不是只新增 `.py`**：Python 导入优先级是 **扩展模块(.pyd) > 源码(.py)**，
   > `.pyd` 还在的话纯 Python 版本永远不会被导入。
   > ⚠️ **重装依赖后需重跑本脚本**（`pip install -r requirements.txt` / 重建 venv 会把原生 `.pyd` 装回来）。
3. 文档补充：`docs/安装指南_环境准备.md` 新增 8.1 节；`backend/requirements.txt` 加提示注释。

**验证**：`app.main` 导入成功（25 个路由）；uvicorn 真实启动成功；
`GET /api/health` 返回 200；`uuid7()` 版本号 7、变体 RFC 4122、同毫秒 5 连发严格递增；
脚本 `--check` / `--restore` / 默认三种模式均通过。

### 文档

- 新增 [`docs/答辩与面试问答手册.md`](docs/答辩与面试问答手册.md)
  —— 基于真实代码核查整理：技术栈选型理由、系统架构、Agent 工具调用链路、
  RAG 全链路与向量检索、10 个真实问题及解决过程、78 道面试题答题要点、
  已知短板与演进路线、数字速记卡。
- **代码注释补全**（只加注释、不改逻辑）：
  - `frontend/src/styles.css`：新增顶部**目录导航**，按 9 大功能区索引 52 个分区
  - `ChatWindow.tsx` / `SettingsModal.tsx`：补文件头（组件属性分组、组件状态、关键函数、踩坑说明）
  - `api/index.ts` / `types.ts`：补文件头（API 模块索引 / 与后端 schema 的同步约定）
  - 其余 24 个前端主要组件与页面：补文件头
  - 注释标签统一为中文（`组件属性（props）` / `组件状态（state）` / `组件事件`）；
    仅保留 `eslint-disable-next-line` 等功能性指令与平台包名不译
  - **零逻辑变更的验证方式**：Python 文件用 `ast` 语法树比对（注释对 AST 完全不可见），
    TS/TSX 剔除注释后逐行比对，再跑 `tsc --noEmit` 兜底

---

## v1.5.0 · 2026-09-13

> 本版含**新增数据库表与增量列**，重启后端会自动迁移，历史数据不丢。

### 新增

- **多 API 接入**：可同时接入多个云端平台（智谱 / 百炼 / DeepSeek / 硅基流动 / OpenAI…），
  各自独立保存 Key 与端点，顶栏按平台分组展示、随时切换。
  新增 `llm_providers` 表、`/api/llm-providers` 增删改查 + 测连（Key 脱敏返回）、
  会话通过 `provider_id` 关联到具体 provider（NULL = 本地 Ollama 或 `.env` 里的默认配置）。
- **数据目录管理**：运行时数据（数据库 / 上传 / 缓存）统一放在 `backend/data/`，
  可在「设置 → 账户 → 数据存储位置」中迁移到任意目录（迁移用 SQLite 在线备份 API），
  也可用 `.env` 的 `DATA_DIR` 指定（改完需重启后端）。
  新增 `GET/POST /api/system/data-dir`。
- **主题系统**：4 套主题（浅色 / 深色 / 护眼 / 高对比）+ 6 档字号 + 自定义强调色。
  主题与字号存**服务器账号**（换设备自动同步），强调色存**浏览器 localStorage**。
- **本地模型自动发现**：扫描本机 Ollama `/api/tags` 列出全部已安装模型，支持切换与删除。

### 修复

- `python-dotenv` **不支持行内注释**，导致 `CLOUD_MODEL=  # 说明` 把注释当成配置值
  （表现为"已配置"假阳性、旧模型名像僵尸一样冒出来）。新增 `config._clean_env()` 兜底
  （值以 `#` 开头视为空），并把 `.env.example` 的注释全部改为独立成行。
- **切换会话模型时被强制改成该 provider 的默认模型**：`sessions.py` 里以
  `payload.model` 优先于 provider 默认模型。
- 设置窗口点击遮罩被误判为关闭（改为只能点右上角叉号关闭）。
- 「深度思考」支持判断过粗（改为按**模型名 + 平台**综合判断）。
- 拉取模型列表时复用全局配置导致**跨平台串台**。

### 文档

- `.env.example` 重写（注释独立成行 + 新增 `DATA_DIR` 等项）。
- `README.md` 更新（多 API、数据目录、目录结构、隐私说明、开源前自查清单）。

### 工程

- `.gitignore` 忽略 `backend/data/` 与 `*.db-shm` / `*.db-wal`。

---

## v1.4 · 2026-09-13

> 本版未单独发版，改动随 v1.5.0 一并提交。**核心是 RAG 分片质量的量化改进**。

### 变更

- **分片算法重写：从「定长窗口」到「语义单元合并」**
  旧实现是定长滑窗 + 窗内找断点，重叠区直接回退固定 80 字符，
  等于**每次重叠都把上一句劈成两半**。
  改为三段式：① `_split_units()` 先把正文拆成最小语义单元（空行分段 → 换行分行 →
  小标题单独成单元 → 按句切分）；② 贪心合并单元到目标长度；③ 重叠改为**回退若干完整单元**。
  实测片段"上一句被腰斩"的比例从 **10.4% 降到 0.4%**。
- **PDF 抽取后的换行归一化 `_clean_pdf_text()`**
  pypdf 按页面视觉排版抽字，一句话常被拆成两三行。新增清洗：剔除页眉页脚（`第 N 页` / `- N -`）、
  英文断词合并、按行尾标点判断"真断段"还是"版式换行"。
  仅对 PDF 生效 —— txt / docx 的换行本来就是语义换行，不能清洗。
- **存量索引重建**：提供单文件「重建索引」接口，供算法升级前的旧数据重新切分向量化。

### 工程

- **SQLite 开启 WAL**：批量重建索引（长时间连续写入 3000+ 条向量）时，
  其他请求会报 `database is locked` —— 默认 `journal_mode=DELETE` 下写会阻塞读。
  改为 `WAL` + `synchronous=NORMAL` + `busy_timeout=30000`，读写可并发。
  > ⚠️ 副作用：写入会先进 `app.db-wal`，未 checkpoint 前不落主库，
  > 不认识 WAL 的简易工具会读到**旧快照**（数据本身没问题）。
  > 排查方法：对比 `file:app.db?mode=ro&immutable=1` 与正常连接的读取结果。

---

## v1.3.0 · 2026-09-12

### 修复

- **云端首字延迟 42 秒 → 0.8 秒**（本版最重要的一处）
  两个原因叠加：① `httpx` 连接池默认 `keepalive_expiry=5s`，空闲即断，
  而用户"打字→发送"的间隔通常 >5 秒，于是**每次提问都在重新握手**
  （走代理重建 TLS 要 20~40 秒）；② `langchain-openai` 会注入自定义 httpx transport，
  **破坏 httpx 的代理自动探测**，所以只改 `http_client(trust_env=True)` 完全没用。
  改法：模块级**全局共享** `httpx.Client` + 保活延长到 10 分钟 + **显式传 `proxy`**
  + `http_socket_options=()` 阻止 transport 注入；配合启动预热与每 60 秒后台保活。
  > 附带能力：代理地址可配置 + **一键自动检测**（扫描本机监听端口并逐个发 CONNECT 探测），
  > 解决"代理软件端口会变、环境变量里是失效地址"的问题。
- 流式结束时回答下方不显示所用模型（`done` 事件补带 provider/model）。
- 云端接入界面重构为 ChatBox 风格的左右双栏。

### 新增

- **云端「深度思考」开关**：流式请求附 `extra_body={"enable_thinking": …}`。
  该参数**仅在流式调用时合法**（非流式传会 400），且目前只有阿里云百炼支持，
  因此按域名判定后再附加。
- **日历：节假日与调休标注**：红色「休」/ 灰色「班」角标，数据源 timor.tech，
  按年缓存到磁盘。节日名由前端固定节日表决定（接口在整段假期内每天返回同一名称）。
- **知识库增强**：单文件上限提到 50MB、正文索引上限 50 万字符；
  新增 **Word（`.docx`）** 支持（标准库 `zipfile` + `ElementTree` 解析，**零新增依赖**）；
  **按知识库设置 Top-K**（规范库调小求精准、资料库调大求召回，未设置的跟随全局默认）。

---

## v1.2.0 · 2026-09-11

### 新增

- **RAG 知识库**：多库管理、上传即自动切分 + 向量化（本地 `bge-m3`，零成本）、
  会话内勾选参与检索、回答下方**标注引用来源**、知识库**关系图**可视化。
- **多模型切换**：界面内切换本地 / 云端模型，模型清单可维护，**每条回答标注所用模型**。
- **图片识别**：上传图片交给多模态模型直接看图。
- **思考过程展示**：推理模型的思考内容折叠展示，与正文分离。

### 工程

- 新增 `VERSION` 文件作为版本号**单一来源**（`/api/health` 返回）。

### 文档

- `README` / 安装指南 / 结构说明按最新版重写，并完成隐私脱敏。
- 项目结构整理（`docs/` 归拢、逐文件说明）。

---

## v1.1 · 2026-09-10

### 变更

- **UI 全站改造为 DeepSeek 蓝白风**：
  - 重建设计令牌（品牌蓝 `#4D6BFE`、中性灰阶、圆角与描边收敛），
    保留 `calc(1rem * N / 14)` 的字号缩放机制
  - 全站 emoji 图标替换为 `lucide-react` 线性图标（涉及 14+ 组件 / 页面）
  - 会话项与功能项改为 pill 激活态、品牌圆形 AI 头像、更圆润的输入框 + 焦点环

### 工程

- 新增依赖 `lucide-react ^1.43.0`。

---

## v1.0.0 · 2026-09-09

### 新增

- 初始版本：
  - 后端：FastAPI + SQLAlchemy + SQLite，AI 层 LangChain / LangGraph
  - 前端：React + TypeScript + Vite
  - 功能：多用户注册登录、多会话管理、流式对话（SSE）、
    工具调用（计算器 / 天气 / 网页搜索）、技能（人设）、待办、计时器、日历
  - 工程：`.gitignore` 排除 `.env` / `app.db` / `uploads` / `venv` 等隐私文件

---

## 附：技术演进要点（速查）

| 时间 | 变化 | 一句话原因 |
|---|---|---|
| v1.1 | emoji → `lucide-react` | 视觉统一、更专业 |
| v1.2 | 引入 RAG 与多模型切换 | 让助理能"翻自己的资料" |
| v1.3 | 云端连接池全局共享 + 显式代理 | httpx 默认只保活 5 秒，重建 TLS 要 20~40 秒 |
| v1.3 | Word 用标准库解析 | `.docx` 本质是 zip + XML，零新增依赖 |
| v1.4 | 分片从"定长窗口"改为"语义单元合并" | 旧法每次重叠都把上一句劈开（实测 10.4% → 0.4%） |
| v1.4 | SQLite 开启 WAL | 默认模式下写会阻塞读，批量建索引时全站报 locked |
| v1.5.0 | 多 API 接入改为独立表 | `.env` 单配置无法同时接多个平台 |
| v1.5.0 | 数据目录可配置 | 便于迁移与备份 |
| v1.5.1 | 纯 Python 顶替被拦的 Rust 原生扩展 | Windows 智能应用控制拒绝加载未签名二进制 |
